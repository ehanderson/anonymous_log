<div class="container">
  <h1>Welcome to Emily's Blog Site</h1>
  <p>Below I have a list of all of the posts you can brows through. Enjoy</p>

  <%= session.keys %>  

  <h2> This will be a drop down of all post names </h2>
  <h3>This will be a way to select by date</h3>

  <h2>This will be a drop down or a list of all tag names</h2>

  <!-- look into submit button -->


  <table>
    <thead>
      <tr>
        <th>Item ID</th>
        <th>Title</th>
      </tr> 
    </thead>
    <tbody>
      <%Post.all.each do |post| %>
        <tr>
          <td><a href="/posts/<%=post.id%>"><%=post.id%></a></td>
          <td><a href="/posts/<%=post.id%>"><%=post.title%></a></td>
      <%end%> 
        <tr>
          <a href="/posts">Here is a link to post page</a>
        </tr>
  </tbody>
</table>




<!-- Tags Below -->

  <table>
    <thead>
      <tr>
        <th>Title</th>
      </tr> 
    </thead>
    <tbody>
      <%Tag.all.each do |tag| %>
        <tr>
          <td><a href="/tags/<%=tag.id%>"><%=tag.id%></a></td>
          <td><a href="/tags/<%=tag.id%>"><%=tag.title%></a></td>
      <%end%> 
        <tr>
          <a href="/tags">Here is a link to tag page</a>
        </tr>
  </tbody>
</table>


  <form action="post_form" id ="post">
    Title:<input type="text" name="title">
    <input type = "submit">
  </form>



  <select>
    <option value="post1">title of post1</option>
    <option value="post2">title of post2</option>
  </select>
